#!/usr/bin/env ruby
require 'machine_time_client'

argv = ARGV.clone
argv.unshift("-o")
argv.unshift("start")
MachineTimeClient::Runner.new.run(argv, :command_name => "machine_command", :program_name => "godigo-machine-start")